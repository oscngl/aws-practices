cloud:
  aws:
    credentials:
      access-key:
      secret-key:
    region:
      static: eu-central-1

application:
  bucket:
    name: spring-boot-aws-s3

spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2MB
      max-file-size: 5MB
      max-request-size: 10MB
